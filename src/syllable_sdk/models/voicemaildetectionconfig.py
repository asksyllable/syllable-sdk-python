"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from enum import Enum
import pydantic
from pydantic import ConfigDict, model_serializer
from syllable_sdk.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from typing import Any, Dict
from typing_extensions import NotRequired, TypedDict


class Mode(str, Enum):
    V1 = "v1"
    V2 = "v2"


class VoicemailDetectionConfigTypedDict(TypedDict):
    r"""Voicemail detection config stored on outbound_campaign.voicemail_detection.

    `mode` selects the (mutually-exclusive) strategy; the remaining fields are detection timeouts.
    Unknown keys are preserved (`extra='allow'`) so the schema stays backwards compatible with blobs
    written before fields were added. See ZOO-7818.
    """

    mode: NotRequired[Nullable[Mode]]
    r"""Voicemail-detection strategy: 'v1' (original check_voicemail/leave_voicemail) or 'v2' (Voicemail Detection 2, which suppresses the turn-0 greeting and uses the greeting tool). The two modes are mutually exclusive. Omitted/null on legacy v1 campaigns is treated as v1."""
    voicemail_detection_overall_timeout: NotRequired[Nullable[float]]
    voicemail_detection_pre_speech_timeout: NotRequired[Nullable[float]]
    voicemail_detection_post_speech_timeout: NotRequired[Nullable[float]]
    voicemail_detection_speech_threshold: NotRequired[Nullable[float]]


class VoicemailDetectionConfig(BaseModel):
    r"""Voicemail detection config stored on outbound_campaign.voicemail_detection.

    `mode` selects the (mutually-exclusive) strategy; the remaining fields are detection timeouts.
    Unknown keys are preserved (`extra='allow'`) so the schema stays backwards compatible with blobs
    written before fields were added. See ZOO-7818.
    """

    model_config = ConfigDict(
        populate_by_name=True, arbitrary_types_allowed=True, extra="allow"
    )
    __pydantic_extra__: Dict[str, Any] = pydantic.Field(init=False)

    mode: OptionalNullable[Mode] = UNSET
    r"""Voicemail-detection strategy: 'v1' (original check_voicemail/leave_voicemail) or 'v2' (Voicemail Detection 2, which suppresses the turn-0 greeting and uses the greeting tool). The two modes are mutually exclusive. Omitted/null on legacy v1 campaigns is treated as v1."""

    voicemail_detection_overall_timeout: OptionalNullable[float] = UNSET

    voicemail_detection_pre_speech_timeout: OptionalNullable[float] = UNSET

    voicemail_detection_post_speech_timeout: OptionalNullable[float] = UNSET

    voicemail_detection_speech_threshold: OptionalNullable[float] = UNSET

    @property
    def additional_properties(self):
        return self.__pydantic_extra__

    @additional_properties.setter
    def additional_properties(self, value):
        self.__pydantic_extra__ = value  # pyright: ignore[reportIncompatibleVariableOverride]

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "mode",
                "voicemail_detection_overall_timeout",
                "voicemail_detection_pre_speech_timeout",
                "voicemail_detection_post_speech_timeout",
                "voicemail_detection_speech_threshold",
            ]
        )
        nullable_fields = set(
            [
                "mode",
                "voicemail_detection_overall_timeout",
                "voicemail_detection_pre_speech_timeout",
                "voicemail_detection_post_speech_timeout",
                "voicemail_detection_speech_threshold",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            serialized.pop(k, serialized.pop(n, None))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val
        for k, v in serialized.items():
            m[k] = v

        return m
