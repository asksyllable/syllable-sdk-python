"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from pydantic import model_serializer
from syllable_sdk.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from typing_extensions import NotRequired, TypedDict


class TranscriptWordConfidenceTypedDict(TypedDict):
    r"""Per-word STT confidence. `word` prefers the provider's punctuated form so the stored words
    read the same way as the transcript they came from.
    """

    word: str
    confidence: NotRequired[Nullable[float]]
    start: NotRequired[Nullable[float]]
    end: NotRequired[Nullable[float]]


class TranscriptWordConfidence(BaseModel):
    r"""Per-word STT confidence. `word` prefers the provider's punctuated form so the stored words
    read the same way as the transcript they came from.
    """

    word: str

    confidence: OptionalNullable[float] = UNSET

    start: OptionalNullable[float] = UNSET

    end: OptionalNullable[float] = UNSET

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["confidence", "start", "end"])
        nullable_fields = set(["confidence", "start", "end"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
